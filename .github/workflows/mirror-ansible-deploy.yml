name: Deploy mirror with Ansible
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
on:
  workflow_dispatch:
    inputs:
      force_schedule_mode:
        description: 'Force playbook to run in "schedule" mode'
        required: false
        type: boolean
        default: false
  workflow_run:
    workflows:
      - Deploy mirror with OpenTofu
    types:
      - completed
    branches:
      - main
  push:
    branches:
      - main
    paths:
      - '.github/workflows/mirror-ansible-deploy.yml'
      - 'mirror/ansible/**'
      - 'mirror/containers/**'
      - 'requirements.txt'
  schedule:
    - cron: '0 0 * * *'  # Every day at midnight
jobs:
  run-ansible:
    runs-on: ubuntu-latest
    environment: mirror
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.0
      - name: Set up inventory, Python version, and SSH key
        env:
          HOST: ${{ secrets.HOST }}
          USER: ${{ secrets.USER }}
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          echo "PYTHON_VERSION=$(cat .python-version)" >> "$GITHUB_ENV"
          if [ -z "$HOST" ] || [ -z "$USER" ]; then
            echo "HOST or USER secret is missing!" >&2
            exit 1
          fi
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          echo "$SSH_PRIVATE_KEY" | base64 --decode > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H "$HOST" >> ~/.ssh/known_hosts
          echo "$HOST ansible_user=$USER ansible_ssh_private_key=~/.ssh/id_ed25519" > inventory
      - name: Cache Ansible Galaxy requirements
        uses: actions/cache@v5.0.5
        with:
          path: ~/.ansible
          key: ansible-galaxy-${{ hashFiles('mirror/ansible/requirements.yml') }}
          restore-keys: |
            ansible-galaxy-
      - name: Set up Python
        uses: actions/setup-python@v6.2.0
        with:
          python-version-file: '.python-version'
          check-latest: true
      - name: Set up uv
        uses: astral-sh/setup-uv@v8.2.0
        with:
          enable-cache: true
          cache-dependency-glob: 'requirements.txt'
      - name: Install Ansible
        run: |
          uv venv
          uv pip install --python .venv/bin/python -r requirements.txt
      - name: Install Ansible Galaxy requirements
        run: |
          uv run ansible-galaxy install -r mirror/ansible/requirements.yml
      - name: Run Ansible Playbook
        env:
          ANSIBLE_CONFIG: mirror/ansible/ansible.cfg
          ANSIBLE_STRATEGY_PLUGINS: ${{ github.workspace }}/.venv/lib/python${{ env.PYTHON_VERSION }}/site-packages/ansible_mitogen/plugins/strategy
          CLOUDFLARED_TOKEN: ${{ secrets.CLOUDFLARED_TOKEN }}
          GHCR_USERNAME: ${{ secrets.GHCR_USERNAME }}
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
          SCHEDULED_RUN: ${{ github.event_name == 'schedule' || github.event.inputs.force_schedule_mode == 'true' }}
          DNS_SEARCH_DOMAIN: ${{ secrets.DNS_SEARCH_DOMAIN }}
          CERTBOT_EMAIL: ${{ secrets.CERTBOT_EMAIL }}
        run: |
          uv run ansible-playbook -i inventory mirror/ansible/playbook.yml --private-key ~/.ssh/id_ed25519
