services:
  headscale:
    image: headscale/headscale:v0.29.4@sha256:8833f828b414c0907b7e5c71da76473216fe17cce0818a166b536ec552c0903f
    command: serve
    restart: unless-stopped
    ports:
      - "443:443/tcp"
      - "3478:3478/udp"
    volumes:
      - /opt/containers/headscale/data:/var/lib/headscale
      - /opt/containers/headscale/config.yaml:/etc/headscale/config.yaml:ro
    tmpfs:
      - /var/run/headscale
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    security_opt:
      - no-new-privileges:true
    networks:
      - app-infra

networks:
  app-infra:
    external: true
