services:
  webmail:
    image: ghcr.io/linagora/tmail-web:release@sha256:35ed4211f4f1f142acbeb725adb35e3daf8114151af1af4daebd580f60e8e7ac
    container_name: webmail
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp
      - /run
    ports:
      - "127.0.0.1:3001:80"
    volumes:
      - ./env.file:/usr/share/nginx/html/assets/env.file
    networks:
      - app-infra

networks:
  app-infra:
    external: true
