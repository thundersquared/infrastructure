services:
  1min-relay:
    image: ghcr.io/thundersquared/1min-relay:v1.0.0
    restart: unless-stopped
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    env_file:
      - .env
    networks:
      - app-infra

networks:
  app-infra:
    external: true
