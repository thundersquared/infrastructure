# Copy this file to .postgres.env and .env.example to .env on web3 before
# deployment.
# POSTGRES_PASSWORD must match the password in DATABASE_URL in .env.

POSTGRES_DB=litellm
POSTGRES_USER=litellm
POSTGRES_PASSWORD=change-me
