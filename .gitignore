.DS_Store

# Container stack secrets — only *.example files are committed
.env
.env.*
!.env.example
!*.env.example
!.env.*.example

.terraform/
*.tfstate
*.tfstate.backup
tfplan
*.tfplan
*.tfvars
*.tfvars.json
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# LiteLLM runtime configuration is managed on web3 alongside its .env file.
/web3/containers/litellm/config.yaml
/web3/containers/litellm/chatgpt-worker-config.yaml
